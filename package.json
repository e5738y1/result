{
  "type": "module",
  "name": "result",
  "version": "1.0.0",
  "description": "Handle errors in place not breaking control flow of your program",
  "main": "dist/result.js",
  "types": "dist/result.d.ts",
  "files": [
    "/dist"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "typedoc --excludeNotDocumented result.ts && tsc"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typedoc": "^0.26.7",
    "typescript": "^5.6.2"
  }
}
